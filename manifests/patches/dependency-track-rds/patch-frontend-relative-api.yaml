apiVersion: apps/v1
kind: Deployment
metadata:
  name: dependency-track-frontend
  namespace: dependency-track
spec:
  template:
    spec:
      containers:
        - name: frontend
          env:
            - name: API_BASE_URL
              $patch: replace
              value: ""
