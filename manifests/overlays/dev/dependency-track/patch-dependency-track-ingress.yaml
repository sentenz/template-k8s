apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dependency-track-hyades
  namespace: dependency-track
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "dependency-track.localhost"
      secretName: dependency-track-tls
  rules:
    - host: "dependency-track.localhost"
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: dependency-track-hyades-api-server
                port:
                  name: web
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: dependency-track-hyades-api-server
                port:
                  name: web
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dependency-track-hyades-frontend
                port:
                  name: web
