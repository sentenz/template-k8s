# Local Kubernetes Configuration

This directory contains configuration and generated credentials for the local Kind development cluster.

- `kind-cluster.yaml` defines the version-controlled Kind cluster topology and host port mappings.
- `kubeconfig.yaml` is generated by `make k8s-setup`, consumed by the Kubernetes Make targets, removed by `make k8s-teardown`, and excluded from version control.
