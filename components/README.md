# Kustomize Components

Reusable cross-cutting Kustomize components belong in this directory.

Examples include network policies, Pod Security labels, observability annotations, common scheduling constraints, and organization-wide metadata. Components should remain environment-neutral; environment selection and composition belongs under `clusters/`.
